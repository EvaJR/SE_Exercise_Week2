package Excercise.model;

import org.junit.Test;

import java.util.ArrayList;
import java.util.List;

public class PouleTest {

    @Test
    public void pouleCreatedAndShuffled() {
        Poule testPoule = new Poule();
        List<Team> testTeams = new ArrayList<>();
        // assert(testTeams.size() == 8 ); error??
        // assert(testTeams.contains(feyenoord)); // how to test what is inside list?
        // how to test if created list is shuffled?

    }
}
